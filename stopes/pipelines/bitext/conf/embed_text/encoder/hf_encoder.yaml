_target_: stopes.modules.preprocess.hf_sentence_encoder.HFTextEncoder

# HuggingFace name of the model
_name: ???
normalize: False
fp16: False

# these are not used, but required as the laser3 uses it. but sentence transformer doesn't need them
# we just set it to nothing
spm_model: ''
spm_vocab: ''
