_target_: stopes.modules.preprocess.preprocess_encode_module.PreprocessEncodeModule

defaults:
  - encode@_here_
  - /moses/standard_conf@preprocess

preprocess:
  output_dir: ${..output_dir}/moses_preprocess
  shards: ${..shards}
  lang: ${..lang}
